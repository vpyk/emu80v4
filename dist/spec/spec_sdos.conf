ifndef NAME
  @NAME = "Специалист SDOS"
endif

define SDOS

# Образ SD-карты (SDOS)
ifndef CFG_SD_IMG
  @CFG_SD_IMG = "spec/sd_spec.img"
endif

@SDOS_ROM = "spec/sdos_hwmpvv.bin"

include "spec/spec.conf"
