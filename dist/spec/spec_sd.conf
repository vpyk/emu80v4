ifndef NAME
  @NAME = "Специалист (SD card / vinxru)"
endif

# Основное ПЗУ и адреса перехвата
@ROM = "spec/spec_m2_sd.rom"

define SD_CARD

# Директория SD-карты (vinxru)
ifndef CFG_SD_DIR
  @CFG_SD_DIR = "spec/sdcard"
endif

include "spec/spec.conf"
